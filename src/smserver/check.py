import os
import yaml 
import requests

import ccache 

# we have a url and a cache to check 
# we need to generate the output to be able to load the updated cache and schedule to download

# ccahce.py is the class that gets exported as a yaml list to be used later and exported.

